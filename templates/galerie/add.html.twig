{# templates/galerie/add.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter une Image</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
